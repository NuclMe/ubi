1)useState вернет массив с 2 елементами - текущее значени еи функция позволяющяя изменять его состояние
меняем стейт - происходит ререндер  
2_ Контролируемый компонент - компонент в котором мы на каждое изменение стейта вызываем функцию которая изменяет стейт

ref - нужен чтобы напрямую DOM изменять ( выделения текста, фокусов и проигрывания медиа.)
ref - это атрибут компонента, значением котоорого является обьект который должкн быть создан или через хук useRef или через метод

1) useRef инициадизируем хук, делаем функцию-обраотчик на конопку, по клику ее вызываем.
На то что реагирует функция 




Тоха вопросы
1) Каждый раз когда меняем стейт в контролируемомо компоненре меняется стейт у нас, следственно ререндер всего компонента? Я правильно понял